# Speech-AI-Forge Docker Environment Configuration

# =============================================================================
# 基础配置
# =============================================================================

# 计算设备配置
# 选项: cpu, gpu, auto
COMPUTE_TYPE=cpu

# 强制使用CPU (设置为 "all" 禁用GPU)
USE_CPU=

# GPU设备ID (仅GPU模式有效)
DEVICE_ID=0

# 禁用半精度计算 (遇到问题时设置为 true)
NO_HALF=false

# =============================================================================
# API服务配置
# =============================================================================

# API服务主机地址
API_HOST=0.0.0.0

# API服务端口
API_PORT=7870

# API工作进程数
API_WORKERS=1

# =============================================================================
# WebUI配置
# =============================================================================

# WebUI服务主机地址
WEBUI_HOST=0.0.0.0

# WebUI服务端口
WEBUI_PORT=7860

# WebUI主题 (light, dark, auto)
WEBUI_THEME=auto

# =============================================================================
# 模型配置
# =============================================================================

# 模型下载源 (huggingface, modelscope)
MODEL_SOURCE=modelscope

# 默认TTS模型
DEFAULT_TTS_MODEL=ChatTTS

# 默认ASR模型
DEFAULT_ASR_MODEL=whisper

# =============================================================================
# 存储配置
# =============================================================================

# 模型存储路径
MODELS_PATH=/app/models

# 数据存储路径
DATA_PATH=/app/data

# 日志存储路径
LOGS_PATH=/app/logs

# =============================================================================
# 性能配置
# =============================================================================

# 最大并发推理数
MAX_CONCURRENT_REQUESTS=4

# 批处理大小
BATCH_SIZE=1

# 内存限制 (GB)
MEMORY_LIMIT=8

# =============================================================================
# 安全配置
# =============================================================================

# API密钥 (留空则不需要认证)
API_KEY=

# 允许的主机列表 (逗号分隔)
ALLOWED_HOSTS=*

# =============================================================================
# 开发配置
# =============================================================================

# 调试模式
DEBUG=false

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 重新加载 (开发模式)
RELOAD=false 